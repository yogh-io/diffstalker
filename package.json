{
  "name": "diffstalker-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "prepare": "git config core.hooksPath .githooks",
    "dev": "cd packages/cli && bun run dev",
    "build": "cd packages/cli && bun run build && cd ../daemon && bun run build",
    "build:prod": "cd packages/cli && bun run build:prod",
    "bundle": "cd packages/cli && bun run bundle",
    "start": "cd packages/cli && bun run start",
    "start:bundle": "cd packages/cli && bun run start:bundle",
    "test": "cd packages/core && bun test src/*.test.ts src/**/*.test.ts && cd ../cli && bun test src/*.test.ts src/**/*.test.ts && cd ../daemon && bun test src/*.test.ts src/**/*.test.ts",
    "test:watch": "cd packages/cli && bun test --watch src/*.test.ts src/**/*.test.ts",
    "lint": "cd packages/core && bun run lint && cd ../cli && bun run lint && cd ../daemon && bun run lint",
    "lint:fix": "cd packages/core && bun run lint:fix && cd ../cli && bun run lint:fix && cd ../daemon && bun run lint:fix",
    "format": "cd packages/core && bun run format && cd ../cli && bun run format && cd ../daemon && bun run format",
    "format:check": "cd packages/core && bun run format:check && cd ../cli && bun run format:check && cd ../daemon && bun run format:check",
    "deps": "cd packages/core && bun run deps && cd ../cli && bun run deps && cd ../daemon && bun run deps",
    "metrics": "bun scripts/collect-metrics.ts",
    "metrics:snapshot": "bun scripts/collect-metrics.ts --save",
    "release": "scripts/release.sh",
    "release:minor": "scripts/release.sh minor",
    "release:major": "scripts/release.sh major"
  }
}
