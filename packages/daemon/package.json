{
  "name": "@diffstalker/daemon",
  "version": "0.4.0",
  "description": "diffstalker daemon: REST API + SSE over @diffstalker/core",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "diffstalkerd": "bin/diffstalkerd"
  },
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "rm -rf dist && tsc -b",
    "start": "bun dist/index.js",
    "test": "bun test src/*.test.ts src/**/*.test.ts",
    "test:watch": "bun test --watch src/*.test.ts src/**/*.test.ts",
    "lint": "eslint src/ && depcruise src/ --config .dependency-cruiser.cjs",
    "lint:fix": "eslint src/ --fix",
    "deps": "depcruise src/ --config .dependency-cruiser.cjs",
    "format": "prettier --write src/",
    "format:check": "prettier --check src/"
  },
  "engines": {
    "node": ">=20.10.0"
  },
  "dependencies": {
    "@diffstalker/core": "workspace:*"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@types/node": "^22.10.7",
    "dependency-cruiser": "^17.3.7",
    "eslint": "^9.39.2",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-sonarjs": "^3.0.6",
    "prettier": "^3.8.0",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.53.1"
  }
}
