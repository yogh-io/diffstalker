// Theme definitions for diffstalker.
// The tables live in @diffstalker/core/view/themes, shared with the web UI;
// the CLI uses them as-is (blessed accepts hex and named ANSI colors alike).

export type { ThemeName, DiffColors, Theme } from '@diffstalker/core/view/themes';
export { themes, themeOrder, getTheme } from '@diffstalker/core/view/themes';
